{
  "name": "zenithui-demo-styles",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind CSS build for the ZenithUI demo application. Models how a consuming app compiles its own utilities alongside the precompiled ZenithUI stylesheet.",
  "scripts": {
    "css:build": "tailwindcss -i ./Styles/app.css -o ./wwwroot/app.css --minify",
    "css:watch": "tailwindcss -i ./Styles/app.css -o ./wwwroot/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
